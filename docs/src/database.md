# 数据库

这里用于整理数据库相关面试题，后续按 SQL、索引、事务、锁、MVCC、慢查询优化、分页优化、连接池、Redis 等方向追加。

## 1、数据库索引为什么能提升查询速度
数据库索引是一种辅助数据结构，可以帮助数据库更快定位数据，避免对整张表做全表扫描。常见关系型数据库中，B+ 树索引非常常见。

::: details 详情
### 没有索引时

如果没有索引，查询可能需要逐行扫描：

```sql
select * from users where name = 'Tom';
```

数据量越大，全表扫描成本越高。

### 有索引时

如果 `name` 字段有索引，数据库可以先在索引结构中查找 `Tom` 对应的位置，再回表读取完整数据。

这类似查字典时先看目录，而不是从第一页翻到最后一页。

### B+ 树索引特点

B+ 树适合数据库索引，原因包括：

- 树高较低，减少磁盘 IO。
- 叶子节点有序，适合范围查询。
- 非叶子节点只保存索引键，能容纳更多索引项。
- 查询、插入、删除都有较稳定的复杂度。

### 索引的代价

索引不是越多越好：

- 会占用额外存储空间。
- 插入、更新、删除时需要维护索引。
- 低选择性字段建索引收益可能不高。
- 复杂查询中索引可能不被优化器选择。

### 常见适合建索引的字段

- 经常出现在 `where` 条件中的字段。
- 经常用于 `join` 的字段。
- 经常用于 `order by` 或 `group by` 的字段。
- 高区分度字段，例如手机号、订单号。

### 面试要点

- 索引通过减少扫描范围提升查询性能。
- 索引会降低写入性能，需要权衡。
- 联合索引要注意最左前缀原则。
- 是否使用索引要结合执行计划分析。
:::

## 2、数据库事务的 ACID 分别是什么
事务是一组要么全部成功、要么全部失败的数据库操作。ACID 是事务的四个核心特性：原子性、一致性、隔离性和持久性。

::: details 详情
### Atomicity 原子性

原子性表示事务中的操作要么全部执行成功，要么全部回滚。

例如转账：

```txt
A 扣款 100
B 加款 100
```

这两个操作必须一起成功或一起失败。

### Consistency 一致性

一致性表示事务执行前后，数据库都要满足业务约束和数据约束。

例如账户余额不能无故增加或减少，唯一索引不能被破坏。

### Isolation 隔离性

隔离性表示多个事务并发执行时，彼此之间不能产生不符合隔离级别预期的干扰。

常见并发问题包括：

- 脏读。
- 不可重复读。
- 幻读。

不同隔离级别解决的问题不同。

### Durability 持久性

持久性表示事务提交后，数据变更应该被持久保存，即使数据库宕机，也应该能通过日志等机制恢复。

### 事务示例

```sql
begin;

update accounts set balance = balance - 100 where id = 1;
update accounts set balance = balance + 100 where id = 2;

commit;
```

如果中间失败，需要执行 `rollback`。

### 面试要点

- 原子性关注全部成功或全部失败。
- 一致性关注业务约束和数据约束不被破坏。
- 隔离性关注并发事务之间的影响。
- 持久性关注提交后的数据可靠保存。
:::
